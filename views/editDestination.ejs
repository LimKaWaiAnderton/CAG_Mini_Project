<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AirPlus</title>
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/editDestination.css">
    <!-- Google Fonts -->
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <!-- Custom JS -->
    <script src="/javascript/javascript.js"></script>
</head>

<body class="editDestinationbackground">
    <!-- Include Navigation Bar -->
    <section class="navbar">
        <%- include('partials/loggedInNav') %>
    </section>
    <section class="container">
        <div class="card">
            <h2>Update Destination</h2>
            <br>
            <% if (country) { %>
            <form action="/editDestination/<%= country.countryID %>" method="POST" enctype="multipart/form-data">
                <div class="form-group text-center">
                    <label for="image">Current Image:</label><br>
                    <input type="hidden" name="currentImage" value="<%= country.countryImage %>">
                    <img src="/images/<%= country.countryImage %>" class="img-fluid mb-3" style="max-width: 200px;"><br><br>
                </div>

                <div class="form-group">
                    <label for="country">Country:</label>
                    <input type="text" id="country" name="country" value="<%= country.countryName %>" required>
                </div>

                <button type="submit" class="btn-primary">Update Destination</button>
            </form>
            <% } else { %>
            <p>No flight found.</p>
            <% } %>
            <a href="/allPopularDestination" class="btn-secondary">Back</a>
        </div>
    </section>
</body>

</html>